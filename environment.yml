name: playbypost
channels:
  - defaults
  - conda-forge
dependencies:
  - python==3.8.*
  - flask==1.1.*
  - slackclient
  - humanize
  - pytest-flask
  - pytest
  - pip
  - pip:
      - slackeventsapi
